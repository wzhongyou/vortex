# Benchmarks will be added here as implementation progresses.
# Each benchmark registers via:
#   add_executable(bm_xxx bm_xxx.cpp)
#   target_link_libraries(bm_xxx PRIVATE vortex benchmark::benchmark)
